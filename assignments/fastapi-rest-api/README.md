# 📘 Assignment: Construindo APIs REST com FastAPI

## 🎯 Objective

Aprenda a criar uma API REST simples utilizando o framework FastAPI em Python. Você irá definir rotas, manipular dados e testar endpoints usando ferramentas como o navegador ou o Insomnia/Postman.

## 📝 Tasks

### 🛠️ Criar uma API REST básica

#### Description
Implemente uma API REST que permita cadastrar, listar e buscar itens (ex: livros, tarefas, produtos) usando FastAPI. Os dados podem ser armazenados em uma lista na memória.

#### Requirements
Completed program should:

- Definir ao menos três rotas: criar item (POST), listar todos os itens (GET), buscar item por ID (GET)
- Utilizar modelos Pydantic para validação dos dados
- Retornar respostas JSON apropriadas
- Permitir testes dos endpoints via navegador ou ferramentas de API

### 🛠️ Documentação automática

#### Description
Aproveite o recurso de documentação automática do FastAPI para explorar e testar sua API.

#### Requirements
Completed program should:

- Disponibilizar a documentação interativa em `/docs` (Swagger UI)
- Permitir testar todos os endpoints pela interface
- Explicar no README como acessar e usar a documentação

